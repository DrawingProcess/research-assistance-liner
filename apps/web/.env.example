# No browser-exposed environment variables are currently required.
# Keep credentials out of NEXT_PUBLIC_* variables. Configure server-only secrets
# with `npx wrangler secret put <NAME>` for the target Worker when they are needed.
